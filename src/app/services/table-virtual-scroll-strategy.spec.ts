import { TableVirtualScrollStrategy } from './table-virtual-scroll-strategy';

describe('TableVirtualScrollStrategy', () => {
  it('should create an instance', () => {
    expect(new TableVirtualScrollStrategy()).toBeTruthy();
  });
});
