import { FilterSlicePipe } from './filter-slice.pipe';

describe('FilterSlicePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSlicePipe();
    expect(pipe).toBeTruthy();
  });
});
